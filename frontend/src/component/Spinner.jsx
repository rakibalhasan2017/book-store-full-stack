import React from 'react';
import './Spinner.css'; // Optional: Styles for the spinner

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="loader"></div> {/* Example spinner animation */}
    </div>
  );
};

export default Spinner;
